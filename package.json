{
  "name": "asd-ems",
  "version": "1.0.0",
  "scripts": {
    "start": "node app.js",
    "build": "cd asd-ems-frontend && npm run build",
    "install-client": "cd asd-ems-frontend && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jest": "^27.2.4",
    "mongo-mock": "^4.1.0",
    "mongoose": "^6.0.8",
    "morgan": "^1.10.0",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.14"
  }
}
